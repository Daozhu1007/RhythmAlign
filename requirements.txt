numpy>=1.21
librosa>=0.9
scipy>=1.7
imageio-ffmpeg>=0.4
PyQt6>=6.4
# The PyQt6 binding of qfluentwidgets. The un-suffixed "PyQt-Fluent-Widgets"
# package is the PyQt5 build and breaks any fresh install of this project.
PyQt6-Fluent-Widgets>=1.3
# Shortcut resolution (ui_main.windows_shortcut_candidates); import is
# guarded, but the dependency must be declared for reproducible envs.
pywin32>=306; sys_platform == "win32"
certifi>=2024.8.30
# Release build tool: the release processes (RELEASE.md, CI, Linux
# packaging) assume PyInstaller in the environment — declared so a fresh
# env from this file is a complete build env (CP-2).
PyInstaller>=6.18
