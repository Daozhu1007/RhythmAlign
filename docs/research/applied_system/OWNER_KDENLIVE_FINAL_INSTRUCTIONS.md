# 你现在要做什么

> 程序版本:**V2**(KDENLIVE-PLACEMENT-V2,2026-09-20)。
> V2 变更:两条音频改为从 **00:03:00** 开始放置(不再从 00:00 开始,原因见步骤 5);新增文末「pair01 重做说明」。其他步骤不变。

你只需要用 Kdenlive 完成 10 个小任务。每个任务约 3–5 分钟。

**重要:做完 10 个任务之前,不要运行、查看或询问任何自动对齐(自动同步)的结果。所有自动系统的最终结果都处于封存状态,必须等你做完这 10 个任务。**

---

## 准备(只做一次)

- Kdenlive 已经装好了(版本 26.08.1)。
- 打开一个命令行窗口(Win+R,输入 `cmd`,回车),然后输入:

```
cd /d D:\Code\RhythmAlign\experiments\applied_system\final_pack\kdenlive
```

- 计时用这个命令(把 pair01 换成当前任务名):

```
python owner_timer.py pair01
```

- 第一次打开 Kdenlive 如果出现设置向导,直接点「关闭」或「确定/跳过」即可。

---

## 每个任务的步骤(pair02 一直做到 pair10,最后按文末说明重做 pair01)

按顺序做 pair02、pair03……一直到 pair10。每个 pair 做同样的操作:

1. 在命令行输入 `python owner_timer.py pair02` (按回车,换成当前任务名)。计时开始后不要再休息。
2. 打开文件夹 `owner_pack\pair02`,里面有两条音频:
   - `recording.wav` —— 现场录音
   - `reference.wav` —— 干净的参考音乐
3. 打开 Kdenlive,新建一个空白工程。
4. 把这两条音频从文件夹拖进 Kdenlive 上方的「项目剪辑箱(Project Bin)」。
5. 这两条都是纯音频文件,必须放在**音频轨道**上:把 `recording.wav` 拖到 **A1** 音频轨道,把 `reference.wav` 拖到 **A2** 音频轨道,**两条都从 00:03:00 位置开始放**。
   - 放置方法:先在时间轴上方的时间码框里输入 `00:03:00:00` 并回车(游标会跳到这个位置),再把剪辑拖到游标附近,它会自动吸附过去。两条都用同样的方法放。
   - **为什么不是 00:00**:时间线零点(最左边)的左侧没有空间。Kdenlive 对齐时可能需要把剪辑移到零点左边;如果从 00:00 开始放,这一步会直接失败。从 00:03:00 开始放,往左往右都足够移动。这个 3 分钟只和音频文件的长度有关,对所有 10 个任务都一样。
   - 新建空白工程默认就有 A1、A2 两条音频轨道。如果只有一条音频轨道,在时间轴左侧任意轨道头上**右键 → 「插入轨道」**,轨道类型选**「音频」**,加出第二条后再放。
6. 在时间轴上**右键点击 `recording.wav` 那一段**,选:
   - **「设置音频辅助线」(Set Audio Reference)**
7. 再**左键单击选中 `reference.wav` 那一段**,右键,选:
   - **「对齐音频到辅助线」(Align Audio to Reference)**
8. 等待 Kdenlive 自己完成对齐。它会自动移动 `reference.wav` 的位置。
   - **不要手动拖动波形去对齐,不要剪断、移动或修改 `recording.wav`。**
   - 如果 Kdenlive 弹出「无法对齐 / 未找到匹配」或错误窗口,**不要手动补救**,记住发生了什么就行。
9. 保存工程:**文件 → 另存为**,存到当前 pair 文件夹里,文件名叫 **`pairNN.kdenlive`**(比如 `pair02.kdenlive`)。
10. 回到命令行,按回车结束计时,然后按提示输入 Kdenlive 的表现:
    - `1` = 对齐正常完成
    - `2` = Kdenlive 说无法对齐 / 未找到匹配
    - `3` = 弹出了错误窗口
    - `4` = 其他情况(写一句备注)
11. 继续下一个 pair(`python owner_timer.py pair03`……一直到 pair10)。做完 pair10 后,再做文末的「pair01 重做说明」。

随时可以查看进度:

```
python owner_timer.py --status
```

---

## 中途可以休息吗

可以。**每按一次回车之间就是一对的用时**,pair 之间的休息不计入。但一旦输入了 `python owner_timer.py pairNN`,就要一口气做完这一个 pair 再休息。

## 遇到问题

- Kdenlive 弹任何窗口、报任何错:照原样记录(选项 2/3/4),不要想办法绕过。
- 如果弹出「Cannot move clip to frame 负数」这类窗口:照原样记录即可。这个提示里显示的帧数可能是实际值的两倍(Kdenlive 的一个显示缺陷),照抄屏幕上显示的数字,不要自己换算。
- 计时器说某个任务已经计时过:跳过它,做下一个(pair01r2 除外:计时器已记录 pair01,所以 pair01 要用 pair01r2 计时)。
- 其他任何疑问:直接停下来,全部做完后一起说。

---

## pair01 重做说明(做完 pair10 之后必做)

pair01 第一次运行时遇到了 V1 程序的一个放置缺陷(两条音频从 00:00 开始放),那次运行已作废,原始记录已封存保留、不做评分。请用同样的输入重做一次:

1. 在命令行输入 `python owner_timer.py pair01r2` (注意结尾是 r2)。计时开始。
2. 打开文件夹 `owner_pack\pair01r2`,里面同样是 `recording.wav` 和 `reference.wav` 两条音频。
3. 按上面的 V2 步骤操作(两条从 00:03:00 开始放 → 设置音频辅助线 → 对齐音频到辅助线),工程保存到该文件夹,文件名叫 **`pair01r2.kdenlive`**。
4. `owner_pack\pair01` 这个旧文件夹**不要打开、不要修改、不要删除**,直接忽略它。
